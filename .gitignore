# Embedded project containers are not tracked by the workspace repository.
project/*/**

# Keep tracked project index at the root.
!project/index.md

# Local environment files
.env
.env.*
!.env.example
